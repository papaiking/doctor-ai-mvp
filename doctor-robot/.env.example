# Pipecat Backend
BACKEND_URL=http://localhost:7860
BACKEND_CONNECT_PATH=/connect

# WebSocket URL (optional - overrides Vite proxy for direct connection)
# Example: WEB_SOCKET_URL=wss://demo.gnixy.com:8443
# The connect path will be auto-appended, so no need to include /connect here.
