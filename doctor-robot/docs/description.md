# Web version

We are developing a voice-based AI agent that can help users with their healthcare related questions.
This is client side application, belong to Gnixy platform running at: https://business.gnixy.com/.

I want to design the UI/UX for this application with following requirements:
- Use the same design language as the Gnixy platform
- Use the same color scheme as the Gnixy platform
- Use the same font as the Gnixy platform
- Use the same icon as the Gnixy platform
- Use the same button style as the Gnixy platform
- Use the same input style as the Gnixy platform
- Use the same output style as the Gnixy platform
- Use the same error style as the Gnixy platform
- Use the same loading style as the Gnixy platform
- Use the same success style as the Gnixy platform

In the UI, there are 2 main components:
- A chat interface where displayed messages from the AI agent and users
- A voice input interface where users can speak their questions and the AI agent will answer them in voice
- A canvas to display the AI agent's interactions with users in voice
- A logo of Gnixy platform
- A button to start/stop conversation
- Footer with the following links: Contact us, Privacy policy
- Center of title is our logo. 
- In voice mode, we dont need text button



Make other design to make UI that run in Enduser device with following requirements:
- Use simplify design
- No header and footer
- Make device screen in landscape style
- Just display the logo, canvas, voice input interface
- An area for displaying the AI agent's interactions with users in voice



Requirement for this application:
- Frontend is built with React and Vite - as in template in project's folder
- Backend: use Pipecat framework to build the backend


I put image file: design/doctor_gnixy.png - it is an doctor ai. Help me to create an idle screen with this doctor ai image moving around.
This screen is opened automatically at the first time this app is run - as default page or it is openned when the main page disconnect socket for  a specific of time

The idle screen:
- Background is black color
- Keep the current text - but with white color
- The doctor image moves in entire screen by straight line, and bounce back when hit edge 

# TABLET version

